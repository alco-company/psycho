# COPILOT.md

## Multi-tenancy landing pages

Being multi-tenant, Psycho will need a way to steer a request to the proper endpoint.

### Prompt

> i am building a multi-tenant blog. how can i make it so that each tenant will have their own homepage

### Result

I wired tenant-specific homepages with subdomains and optional custom domains. Changes made:

* Data
  * Added migration to extend tenants with slug, homepage_title, homepage_body, plus unique index.
  * Added Domain model/table (host unique, belongs_to :tenant).

* Models
  * Tenant: has_many :domains, validations incl. slug format/uniqueness.
  * Domain: belongs_to :tenant, validates host uniqueness.
  * Current: holds Current.tenant.

* Controller setup
  * ApplicationController: before_action to set Current.tenant via subdomain slug, or via Domain host for custom domains.
  * Tenant::HomeController#show uses Current.tenant and renders per-tenant homepage.
  * PublicController#landing as global root (no tenant).
  * DomainsController CRUD to manage custom domains.
  * TenantsController strong params include slug/homepage fields; success notice points to lvh.me subdomain in dev.

* Routes
  * Subdomain root => tenant/home#show.
  * Custom domain root (if Domain matches) => tenant/home#show.
  * Fallback root => public#landing.
  * resources :domains added.

* Views
  * app/views/tenant/home/show for tenant homepage.
  * Public landing page.
  * Simple Domain CRUD views.

* Dev config
  * Allowed lvh.me subdomains for local testing.

### Actions

* Run database migrations.
* Create a tenant with a unique slug (e.g., “acme”).
* Visit http://acme.lvh.me:3000 to see that tenant’s homepage.
* Optionally create a Domain record (host -> tenant) and point DNS to your app; visiting that host will show the same tenant homepage.

### Prompt

> add tests for controllers, models, and the views

### Result

I added tests covering models, controllers, routing, and the tenant homepage view:

* Fixtures

  * tenants.yml: added slug and homepage fields
  * domains.yml: added two domain records

* Model tests

  * test/models/tenant_test.rb: validates presence, uniqueness, and slug format
  * test/models/domain_test.rb: validates presence and host uniqueness

* Controller tests

  * tenants_controller_test.rb: create/update include slug/homepage fields
  * domains_controller_test.rb: CRUD flow for domains
  * public_controller_test.rb: root renders landing without tenant

* Routing test

  * tenant_home_routing_test.rb: asserts subdomain and custom-domain root routes to tenant/home#show

* System test

  * tenant_home_system_test.rb: visits tenant root via lvh.me subdomain and asserts homepage content

### Actions

Run migrations, then tests:

* bin/rails db:migrate
* bin/rails test
* bin/rails test:system

### Prompt

> run tests

### Result

All tests are passing.

Ran:

* bin/rails db:migrate && bin/rails db:migrate RAILS_ENV=test
* bin/rails test

Fixes during run:

* Tightened slug validation to disallow consecutive dashes.
* Moved controller to Tenants::HomeController and updated routes.
* Added missing view at app/views/tenants/home/show.html.erb.
* Adjusted routing tests to make real requests instead of assert_routing and avoided 406s.

### Prompt

> that works but i production i cannot edit config/environments/production.rb and restart the server everywhen a user signs up and adds a domain

### Result

Use a dynamic allowlist in production:

  * Add a permissive host pattern in an initializer so you don’t need to touch production.rb:
    * host_authorization.rb
    * In production, allow DNS-like hosts via regex.

  * Enforce real allowlisting at the app layer:
    * In ApplicationController, after setting Current.tenant, check:
      * Host matches a Domain record, or
      * Subdomain matches a Tenant slug, or
      * Is your primary app host (for the global landing).
    * Otherwise render 404. No server restart needed when adding domains.

Operational flow:

  * User adds a domain in-app -> a Domain record is created.
  * Next request to that host passes HostAuthorization and is accepted by enforce_known_host!.
  * No config change or restart required.

## Dynamic layouts

Tenants may likely want their own layout.

### Prompt

> can each tenant have their own layout - adding HTML into a textarea in a themes model, and let tenants has_one :theme, and domains has_one :theme - making it possible to add layouts dynamically instead of uploading new layouts to the views/layouts

### Result

Yes. I added dynamic themes so each tenant (or domain) can have its own HTML layout stored in the database:

* Data model
  * Theme model with fields: name, html_layout (textarea content), css, js
  * Associations:
    * Tenant has_one :theme
    * Domain has_one :theme
    * Theme belongs_to :tenant or :domain (mutually exclusive, validated)

* Request context
  * Current now also tracks Current.domain
  * ApplicationController sets Current.domain and Current.tenant

* Dynamic layout
  * Global layout set to layouts/dynamic
  * If a theme exists, it renders theme.html_layout with a simple {{yield}} placeholder replaced by the view’s content
  * Inlines theme.css and theme.js if present
  * Precedence: domain.theme if present, else tenant.theme

How to use:

  * Create a Theme for a tenant or for a domain:
    * name: “Default”
    * html_layout: e.g.:<!doctype html> <html> <head><title>{{yield}}</title></head> <body> <header>My Header</header> <main>{{yield}}</main> <footer>©</footer> </body> </html>
    * css/js: optional customizations

  * If both domain and tenant have themes, the domain’s theme wins for that request.

### Prompt

> yes to CRUD UI no to seeds

### Result

Theme CRUD UI added:

* Routes
  * resources :themes

* Controller
  * ThemesController with index/show/new/edit/create/update/destroy
  * Strong params: name, html_layout, css, js, tenant_id, domain_id

* Views
  * themes/index.html.erb list with tenant/domain columns
  * _form.html.erb with dropdowns for Tenant or Domain, textarea editors for HTML/CSS/JS
  * themes/new.html.erb, themes/edit.html.erb, themes/show.html.erb, _theme.html.erb

* Layout
  * layouts/dynamic.html.erb renders DB-backed HTML with {{yield}} placeholder, inlines CSS/JS

* Precedence: Domain theme over Tenant theme

Run migrations and test:

  * bin/rails db:migrate
  * Visit /themes to create and attach themes
  * Load a tenant or custom domain; it should render using that theme’s layout

### Prompt

> yes - but first a small change (because i made an error) the tenant has_many themes (one for landingpage, another for the blog, products, etc) and a select form attribute named default_theme (but if no theme exist than the app default theme should be used. the app default theme should be seeded and assigned to the first tenant which also should be seeded with a name = "psychOS" ). domains also has a select form attribute named theme - the select will hold all the themes that belongs_to :tenant

### Result

Done. Updated to support:

* Tenant has_many themes and optional default_theme
* Domain belongs_to theme (optional), choosing from the tenant’s themes
* Theme belongs_to tenant (required)
* Dynamic theme resolution order: domain.theme -> tenant.default_theme -> app default theme (psychOS tenant’s theme)

Changes:

* DB migrations
  * add domains.theme_id and tenants.default_theme_id
  * drop themes.domain_id and require themes.tenant_id

* Models
  * Tenant: has_many :themes, belongs_to :default_theme
  * Domain: belongs_to :theme
  * Theme: belongs_to :tenant

* Controller and layout
  * current_theme helper returns the effective theme
  * layouts/dynamic uses current_theme for html_layout/css/js

* Forms
  * Tenants form: default_theme select from tenant.themes
  * Domains form: theme select (currently lists all themes; can be filtered client-side by selected tenant)

Next:

* Run migrations: bin/rails db:migrate
* Seed app defaults you requested (psychOS tenant and a default theme), or tell me to add a one-time rake task/seed.

### Prompt

> add tests

### Result

`bin/rails db:migrate && bin/rails db:migrate RAILS_ENV=test`

### Prompt

> we should test domains with and without a theme

### Result

Added integration tests to cover domains with and without a theme:

* dynamic_theme_rendering_test.rb
  * domain without theme falls back to tenant default theme
  * domain with theme overrides tenant default

Run:

* bin/rails db:migrate
* bin/rails test dynamic_theme_rendering_test.rb

### Prompt

> filter the Domain form’s theme dropdown to only show the selected tenant’s themes.

### Result
Done. The Domain form now filters Theme options by the selected Tenant.

What I changed:

* _form.html.erb
  * Wired a Stimulus controller themes-filter.
  * Initial server-side options show only domain.tenant.themes (or none if no tenant yet).
  * Added a JSON dataset of all tenants’ themes for client-side filtering.

* themes_filter_controller.js
  * Populates the Theme select when the Tenant select changes, preserving selection if still valid.

* Importmap/Stimulus setup already supports auto-loading controllers, so no extra wiring needed.

How to use:

Go to New/Edit Domain, pick a Tenant; the Theme dropdown will update to only that tenant’s themes.
Leave Theme blank to use the tenant/app default.

### Prompt

> Failure:
> TenantsTest#test_should_create_tenant [test/system/tenants_test.rb:27]:
> expected to find text "Tenant was successfully created" in "New tenant\n2 errors prohibited this tenant from being saved:\nSlug can't be blank\nSlug must be lowercase letters, numbers, and single dashes (no leading/trailing or consecutive dashes)\nName\nSubdomain\nLowercase letters, numbers and dashes. Will be used as subdomain (e.g., acme.lvh.me)\nDefault Theme\n-- app default --\nPlan\nPlan changed at\nPlan expires at\nTax number\nEmail\nLocale\nTime zone\nHomepage title\nHomepage body\nBack to tenants"

bin/rails test test/system/tenants_test.rb:13

### Result

Fixed.

* Tenants system test: added Subdomain (slug) fill-in so creation passes.
* Tenant home system test: replaced host! with Capybara.app_host to target acme.lvh.me.

### Prompt

> add basic validations/previews in the form (live preview)

### Result

Added live preview and basic validation to the Theme form.

Changes:

* themes/_form.html.erb:
  * Wired a Stimulus controller (theme-preview) with live iframe preview.
  * Preview updates as you type HTML/CSS/JS.
  * Warns if {{yield}} is missing and disables the submit button until present.
* app/javascript/controllers/theme_preview_controller.js:
  * Renders sample content into {{yield}}, injects CSS/JS into the preview.

Also kept Domain form’s tenant-filtered theme dropdown from earlier. All tests pass.

### Prompt

>  have lost all tailwind formating - the application-8d441ae0.css has no content besides an initial comment section, when I go to customdomain:3000/tenants, or subdomain.lvh.me:3000/tenants, or lvh.me:3000/tenants - no tailwind in any case

### Result

Cause:

* Your layout only linked “application.css”, which is empty. Tailwind builds to “tailwind.css”.

Fix applied:

* Added stylesheet_link_tag "tailwind" to dynamic.html.erb so Tailwind loads everywhere.

Verify:

* Reload a tenants page and check the Network tab for tailwind.css (should have content).
* If still missing, run the Tailwind build/watch in dev:
  * bin/rails tailwindcss:build
  * or keep it running: bin/rails tailwindcss:watch

Notes:

* “application-*.css” being mostly comments is expected since Tailwind utilities live in “tailwind.css”.

### Prompt

> add seeding first tenant and first theme belonging to first tenant

### Result

Seed added.

* db/seeds.rb:
  * Creates tenant “psychOS” with slug “psychos” if missing.
  * Creates “App Default” theme for that tenant and assigns it as default.
  * Idempotent; safe to run multiple times.

* Seeded both dev and test DBs.
* Updated WORKLOG entry accordingly.

### Prompt

> if i go to a url (alco.lvh.me:3000) in the browser which is not a valid tenant slug/custom domain the browser redirects me to many times - add a fallback landingpage and a test

### Result

Added a safe fallback to avoid redirect loops and a test.

Changes:

* routes.rb: Only route subdomain root to tenants/home when the subdomain slug exists. Otherwise, requests fall through to the global root (public#landing).
* test/integration/fallback_landing_test.rb: Verifies unknown subdomain and unknown custom domain render the landing page without redirects.

## Blog

Each tenant will have their own blog with permalinks to each post. The blog will have a landingpage/endpoint listing a select set of blog posts 

### Prompt

> add a blog model. tenants may have many blogs (usually they will not but we will cater for it). a blog will have a setting for allowing comments. This setting should be an enum with possible values being :none, :tenant_only, :users, :with_email, :all

### Result

Done.

* Added Blog model and migration with:
  * belongs_to :tenant
  * enum :comments_setting with values: none, tenant_only, users, with_email, all
  * default comments_setting is :none
  * unique index on [tenant_id, title]
  
* Updated Tenant to has_many :blogs.

* Seeds: psychOS gets a default “Main Blog” (others unaffected).

* Tests: added BlogTest and fixed enum API/prefix to avoid collisions. All tests pass.

### Prompt

> add CRUD UI and tests

### Result

CRUD UI and tests added for Blogs.

Changes:

* routes: added resources :blogs
* BlogsController: standard CRUD with strong params
* Views: blogs/index, new, edit, show, _form
* Tests: controller and system tests for full CRUD flow

All tests pass.

### Prompt

> add a landingpage for each tenant's blog

### Result

Added a tenant-scoped blog landing.

What you now have:

* Tenant blog routes on tenant-aware hosts:
  * GET /blog → shows the tenant’s single blog directly, or lists all blogs if multiple.
  * GET /blog/:id → shows a specific blog for that tenant.

* Controller: Tenants::BlogsController with actions default and show.
* Views: index.html.erb and tenants/blogs/show.html.erb.
* Tenant homepage now shows a “Visit blog” link if the tenant has any blogs.

Notes:

* Global admin CRUD at /blogs is unchanged.
* Works for both subdomain tenants and custom domains.

### Prompt

> add blog post scaffold with attributes title, content (ActionText rich text) published_at date and tests

### Result

Added Post scaffold with rich text and tests.

What’s included:

* Model: Post belongs_to Blog; has_rich_text :content.
* Migration: posts table (blog_id, title, published_at).
* Controller: strong params permit content; index eager-loads rich text.
* Views: form uses rich_text_area; show/index render content; includes published_at.
* Styles: added actiontext.css link to dynamic layout.
* Fixtures: blogs.yml added for post fixtures to reference.
* Tests: controller and system tests generated by scaffold pass.

### Prompt

> on the tenant blog list the latest 5 posts in published_at order descending, just after the description in a div by itself, and test no posts, 4 posts, and 6 posts

### Result

Implemented latest posts on tenant blog list and tests.

Changes:

* Tenants::BlogsController#default always renders index and preloads posts.
* tenants/blogs/index: under each blog’s description, render a div with the latest 5 posts ordered by published_at desc (fallback to created_at), or “No posts yet”.

Tests:

* Added tenant_blog_latest_posts_test covering:
  * No posts → shows “No posts yet”.
  * 4 posts → shows all 4 in order.
  * 6 posts → shows latest 5, excludes the 6th.

